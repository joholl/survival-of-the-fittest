// Controlls ticks
use super::model::Model;

const BOARD_WIDTH: usize = 200;
const BOARD_HEIGHT: usize = 100;

pub fn start() {
    let model: Model = Model::new(BOARD_WIDTH, BOARD_HEIGHT);

    // TODO: handle ticks
}

pub fn stop() {}
